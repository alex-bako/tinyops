# Plan — M1.S1 Hosted email delivery (docs-only)

## Objective
Give the operator a checklist to make invite and sign-in email reach real mailboxes on the hosted Supabase projects (INV-9), without any secret entering the repo.

## Acceptance mapping (docs/roadmap/milestones/M1.md → evidence)
- Repo change limited to a section in `docs/deployment.md` → this slice adds "Invite and sign-in email" under "Supabase Projects".
- Custom SMTP, templates, redirect allowlist → each is a numbered step; templates point at `supabase/templates/*.html` added in T3 so hosted links match `/auth/callback`.
- Secrets entered by the user → the doc says so explicitly; nothing else changes.
- Exit evidence (email delivered on staging, T2 flow completing) is an operator action on hosted infrastructure and cannot be produced from this repo; the doc's step 4 is the verification script for it.

## Required checks
- `git diff --check` (whitespace) — docs only, no code.

## Review policy
Specialist `aw-refuter` (opus) + cross-model substitute `aw-refuter` (sonnet), bounded to the doc section.
